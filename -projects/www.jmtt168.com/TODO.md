# TODO — 日后优化方向

## 功能完善

- [ ] **翻页范围可配置** — 当前 `range(1, 2)` 硬编码，改为命令行参数或配置文件
- [ ] **支持更多分类** — 当前固定 `/anime/comics/list/15`，可扩展为多分类遍历

## 下载稳定性

- [ ] **重试机制** — 单张图片下载失败后无重试，可加 `retry` 装饰器
- [ ] **请求超时** — `requests.get()` 未设 `timeout`，网络卡住会永久阻塞
- [ ] **断点续传** — 中断后重新运行会从头爬，无法跳过已下载图片
- [ ] **进度显示** — 无进度条，下载量大时不直观

## 工程化

- [ ] **日志替代 print** — 当前全用 `print` 输出，改用 `logging` 模块便于排查
- [ ] **配置文件分离** — 目标 URL、线程数、分页范围等硬编码参数外提
- [ ] **Cookie/Header 管理** — 部分网站需登录态，预留请求头配置入口
- [ ] **异常队列清理** — 某级队列异常退出时，其他线程可能永久阻塞在 `.get()`
