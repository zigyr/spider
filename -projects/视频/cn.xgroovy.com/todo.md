# TODO — 日后优化方向

## 用户体验

- [ ] 对 page_urls 进行页面回显（显示当前下载第几个 / 共几个）
- [ ] 显示最终爬取文件的文件大小（下载完成后输出 MB）

## 下载稳定性

- [ ] 下载失败重试（单线程失败不影响其他线程，失败线程自动重试）
- [ ] 下载进度条（tqdm 或自定义，实时显示每线程进度）
- [ ] 断点续传（检测文件已存在则跳过）

## 功能扩展

- [ ] 线程数可配置（当前写死 8，改为参数传入或按文件大小动态调整）
- [ ] chunk_size 按文件大小自适应（当前固定 1MB）
- [ ] 支持选择视频清晰度（同一页面可能有多个 video_source）
- [ ] 已下载记录持久化（避免重复下载同名视频）

## 工程化

- [ ] 配置文件抽离（headers、线程数、保存路径写入 config）
- [ ] 下载日志（记录成功/失败列表，含视频标题和 URL）