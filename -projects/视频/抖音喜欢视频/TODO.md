# TODO — 日后优化方向

## 功能完善

- [ ] **图文/图集筛查** — 喜欢列表中可能包含图文和图集（`images` 字段），目前 `if "video" in aweme` 静默跳过，需增加分类统计 + 选择性下载图文
- [x] **API 自动翻页** — ~~利用响应中的 `max_cursor` 字段自动翻页~~ → 已通过 DrissionPage 接口监听实现（20260801），但程序化触发翻页仍待突破
- [x] **JSON 自动解析** — ~~直接从 API 响应流式提取 URL~~ → DrissionPage + `tab.listen.wait()` 实时拦截，省去手动复制步骤（20260801）

## 下载稳定性

- [x] **下载重试 + 超时** — ~~`requests.get(url)` 无 timeout、无 retry~~ → aiohttp + 指数退避重试 3 次（20260802）
- [ ] **断点续传** — 记录已下载的视频 ID（`aweme_id`），中断后跳过已下载的
- [x] **进度显示** — ~~大列表下载时显示 `n/m` 进度~~ → tqdm 进度条 + 成功/失败统计（20260802）

## 视频质量

- [ ] **清晰度自动选择** — `play_addr.url_list` 有多个 CDN 地址，对比 `bit_rate` 字段自动选最高码率
- [ ] **无水印版本** — 探索是否可通过参数获取无水印视频源（部分接口有 `play_addr_h264` 等备选）

## 工程化

- [x] **异步并发下载** — ~~`aiohttp` 替代 `requests`~~ → aiohttp + Semaphore(10) 并发下载，已实现（20260802）
- [ ] **Cookie 过期检测** — 自动检测返回 `status_code`，cookie 失效时主动提示
- [ ] **配置文件** — 将 cookie、输出路径等抽到独立的 config
- [ ] **下载日志** — 记录每次下载的视频名称、URL、成功/失败状态
