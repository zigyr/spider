# 2026-07-17

## -course | 正则表达式全模块实战
- **文件**: `-course/崔庆才_b站52讲视频课/正则/demo1.py`（修改）、`demo2.py`~`demo11.py`（新增）、`正则速查手册.md`（新增）
- **内容**: 从 re.match 基础匹配、量词（`* + {n}`）、贪婪/非贪婪（`.*?`）、标志位（`re.S`）、HTML 解析实战（search/findall/sub）、compile 预编译到 groups 分组提取，覆盖正则全链路；速查手册汇总字符类/量词/锚点/分组/标志位及爬虫高频模式
- **技术点**: `re.match`, `re.search`, `re.findall`, `re.sub`, `re.compile`, `re.S`, 分组捕获

## 项目结构 | 架构重组与 Skills 体系建立
- **文件**: 目录重命名（`01-basics→_recipes`, `02→-course`, `04→-projects`, 删除 `03-dll`），`CLAUDE.md`（修改），`README.md`（修改）
- **内容**: 将项目重新组织为 -course（学）→ -projects（练）→ _recipes（经验沉淀）的线性结构；建立 Skills 注册机制，每个 skill 在 CLAUDE.md 中有 ⚙️ 触发说明段落 + 🧩 注册表索引
- **技术点**: 项目架构设计, `git mv` 重命名追溯, Skill 规范编写

## Skills | 三个自动化 Skill 就位
- **文件**: `.claude/skills/journal.md`（修改）、`.claude/skills/snippet-extract.md`（新增）、`.claude/skills/rename-folder.md`（新增）
- **内容**: journal（git 操作后自动写日志）、snippet-extract（脚本经验化→移入 _recipes）、rename-folder（两模式：demo 从零命名 / 已有名校准统一，格式为「难度-功能」中文名）
- **技术点**: Claude Code Skill 机制, 难度分级框架（基础/进阶/实战）

## -course | 模拟登录五种策略
- **文件**: `-course/崔庆才_b站52讲视频课/模拟登录/demo1~5.py`（重命名为 基础-无状态请求/进阶-手动传递Cookie/进阶-Session自动管理/进阶-Selenium取Cookie/实战-JWT令牌认证）
- **内容**: 从无状态 POST 暴露 cookie 丢失问题 → 三种保持登录策略（手动拦截、Session 自动管理、Selenium 驱动）→ 现代 JWT token 认证，形成完整递进链
- **技术点**: `requests.Session`, `allow_redirects`, Selenium cookie 注入, JWT Authorization header

## -course | 多进程九步递进
- **文件**: `-course/崔庆才_b站52讲视频课/多进程/demo1~9.py`（重命名为 基础×2/进阶×6/实战×1）
- **内容**: `Process` API 入门 → 自定义进程类 → 守护/join/terminate 生命周期控制 → Lock 互斥锁 → Queue+Semaphore 生产者消费者模式，覆盖多进程全链路
- **技术点**: `multiprocessing.Process`, `daemon`, `join`, `terminate`, `Lock`, `Semaphore`, `Queue`
