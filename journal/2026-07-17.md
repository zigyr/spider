# 2026-07-17

## -course | 正则表达式全模块实战
- **文件**: `-course/崔庆才_b站52讲视频课/正则/demo1.py`（修改）、`demo2.py`~`demo11.py`（新增）、`正则速查手册.md`（新增）
- **内容**: 从 re.match 基础匹配、量词（`* + {n}`）、贪婪/非贪婪（`.*?`）、标志位（`re.S`）、HTML 解析实战（search/findall/sub）、compile 预编译到 groups 分组提取，覆盖正则全链路；速查手册汇总字符类/量词/锚点/分组/标志位及爬虫高频模式
- **技术点**: `re.match`, `re.search`, `re.findall`, `re.sub`, `re.compile`, `re.S`, 分组捕获
