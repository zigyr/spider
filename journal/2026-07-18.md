# 2026-07-18 学习日志

## -projects | B站视频 DASH 流下载
- **文件**: `-projects/视频/b站视频/实战-DASH流视频下载.py`（新增）、`API参数参考.md`（新增）、`README.md`（新增）
- **内容**: 完整 B站视频爬虫——HTML 提取 `__INITIAL_STATE__` → WBI 签名（mixin_key 重排 + MD5）→ playurl API 请求 → DASH 流解析取最高码率 → m4s 分片下载 → ffmpeg 合并；API 参数参考覆盖 qn/fnval/fnver/fourk 及 WBI 签名机制全流程
- **技术点**: DASH 流, WBI 签名重排表, fnval 位掩码, ffmpeg 音视频合并

## _recipes | JSON 格式化写入
- **文件**: `_recipes/JSON数据格式化写入.py`（从 B站视频项目迁移）
- **内容**: `json.dump(data, f, ensure_ascii=False, indent=4)` — 将 JSON 以可读格式写入文件，避免 Unicode 转义
- **技术点**: `json.dump`, `ensure_ascii` 控制中文显示, `indent` 缩进可读

## -projects | 抖音喜欢视频批量下载
- **文件**: `-projects/视频/抖音喜欢视频/实战-批量下载喜欢视频.py`（新增）、`基础-API接口探测.py`（新增）、`1.md`（新增）、`README.md`（新增）、`TODO.md`（新增）
- **内容**: 完整抖音喜欢列表下载管线——浏览器抓包获取 `/aweme/v1/web/aweme/favorite/` API 响应 JSON → 解析 `aweme_list` 过滤图文提取视频 URL → requests 批量下载；API 探测脚本验证 cookie 有效性和接口连通性；已成功下载 19 个视频
- **技术点**: 抖音 Web API, `aweme_list` 解析, `play_addr.url_list`, 图文过滤

## _recipes | URL编解码 + JSON提取视频URL
- **文件**: `_recipes/URL编解码.py`（新增）、`_recipes/从JSON提取视频URL.py`（新增）
- **内容**: 从抖音喜欢视频项目提炼两个通用片段——`urllib.parse.quote/unquote` 处理搜索关键词的百分号编解码；遍历嵌套 JSON 的 `aweme_list` 按条件过滤视频类型并提取 `url_list`
- **技术点**: `urllib.parse.quote`, `urllib.parse.unquote`, JSON 嵌套遍历, 条件过滤

## -projects | cn.xgroovy.com 项目整理
- **文件**: `-projects/视频/cn.xgroovy.com/实战-HTTP-Range多线程下载.py`（重命名自 main.py）、`README.md`（新增）、`todo.md`（修改）
- **内容**: 项目标准化收尾——main.py 重命名为统一格式 `实战-HTTP-Range多线程下载.py`；补全含日期和作者的 README；TODO 从 2 条扩充至 4 类 10 条
- **技术点**: 项目整理, `{难度}-{功能}.py` 命名规范, README 模板

## 体系 | project-polish skill
- **文件**: `.claude/skills/project-polish.md`（新增）、`CLAUDE.md`（修改）
- **内容**: 新增项目整理四件套 skill——触发后自动执行文件重命名检查、经验化脚本识别、README 补全、TODO 扩充四步标准化流程；CLAUDE.md 同步 ⚙️ 段落和 🧩 注册表
- **技术点**: Skill 体系扩展, 自动化工作流, 项目标准化
