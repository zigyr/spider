# 2026-07-24 学习日志

## AcFun 搜索爬虫

- **新建项目** `-projects/视频/AcFun/`
- 实现 AcFun 搜索页爬虫：关键词"御姐" → 搜索页 → 从 `bigPipe.onPageletArrive` 块提取 JSON → 解析 `pagelet_video` html → xpath 提取标题/作者/封面/时长/播放量/弹幕数/发布时间
- 自动下载封面到 `out/` 目录
- 这是第三个视频平台的实战（抖音 → B站 → AcFun），模式已形成：搜索 → JSON 解析 → 封面下载

## B站目录整理

- `b站视频/` → `b站/` 简化为两字名称，与其他项目（AcFun、抖音喜欢视频）命名风格统一

## 抖音工具补充

- 新增 `timestamp.py`：`time.time()` ↔ `datetime.fromtimestamp()` 双向转换
- 新增 `视频.md`：抖音 API 返回 JSON 的字段映射速查（视频/作者/音乐路径）
- `基础-API接口探测.py`：补充 `# 固定值` 注释标注不变量
